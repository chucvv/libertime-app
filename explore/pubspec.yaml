name: explore
description: explore module
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0

  equatable: ^1.2.5

  dartz: ^0.9.2

  chopper: ^3.0.6

   # Bloc for state management
  bloc: ^6.1.0
  flutter_bloc: ^6.1.0

  # Implement generic validation
  verify: ^1.1.1

  # make work with union easier
  freezed_annotation: ^0.12.0

  # Service locator
  get_it: ^5.0.1

  share_module:
    path: ../share_module

  share_ui:
    path: ../share_ui
  
  #Converters & built value integration chopper
  built_value: ^7.1.0

dev_dependencies:
  lint: ^1.3.0
  flutter_test:
    sdk: flutter
  built_value_generator: ^7.1.0
  build_runner: ^1.10.2 #-> flutter packages pub run build_runner watch --delete-conflicting-outputs
  chopper_generator: ^3.0.3
  freezed: ^0.12.2

flutter: