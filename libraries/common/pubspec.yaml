name: common
description: common package
version: 0.0.1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  data_connection_checker: ^0.3.4
  equatable: ^1.2.5
  dartz: ^0.9.2
  freezed_annotation: ^0.12.0
  intl: ^0.16.1

dev_dependencies:
  pedantic: ^1.9.2
  flutter_test:
    sdk: flutter

  build_runner:
  freezed: ^0.12.7

flutter:
